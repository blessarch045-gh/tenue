# スキル: Gemini 投稿画像作成

> トリガー: 「（週の）画像作って」「Geminiで画像作って」「今週の画像を作って」など

---

## 概要

Instagram投稿用の画像を **Gemini API（Imagen 3）** でバッチ生成し、
**ブラウザ操作・手動ダウンロード不要** でプロジェクトフォルダに直接保存する。

```
投稿予定ファイル → プロンプト抽出 → API呼び出し → 自動保存
```

---

## 初回セットアップ（一度だけ）

1. [Google AI Studio](https://aistudio.google.com/apikey) で API キーを取得
2. `.env` の `GEMINI_API_KEY=your_gemini_api_key_here` を実際のキーに差し替える
3. 動作確認:
   ```bash
   cd /Users/bless-arch/Desktop/tenue
   python3 tools/generate_image.py \
     --prompt "春の公園を歩く女性の後ろ姿" \
     --output "/tmp/test.png"
   ```

---

## 実行フロー

### ステップ1: 投稿予定ファイルを読み込む

```
対象ファイル例: マーケティング/Instagram/投稿予定_Week3_3月30日〜4月5日.md
```

- ファイルから「画像プロンプト」セクションを全て抽出
- 未生成の画像（`マーケティング/Instagram/画像/` にないもの）を確認
- 生成枚数・構成をユーザーに報告してから開始

例:
```
今週の未生成画像は以下です。API生成を開始します。
- 4/1（水）カルーセル: 5枚
合計: 5枚
```

### ステップ2: Bashコマンドで生成・保存

```bash
cd /Users/bless-arch/Desktop/tenue

python3 tools/generate_image.py \
  --prompt "[投稿予定ファイルのプロンプト]" \
  --output "マーケティング/Instagram/画像/[ファイル名].png" \
  --aspect [1:1 or 9:16] \
  --count 1
```

**アスペクト比の指定:**
| 投稿フォーマット | `--aspect` |
|----------------|-----------|
| カルーセル・静止画 | `1:1` |
| リール（動画表紙）・ストーリーズ | `9:16` |

**カルーセル（複数枚）は1枚ずつ生成:**
```bash
# 5枚のカルーセルなら5回実行（各プロンプトが異なるため）
python3 tools/generate_image.py --prompt "..." --output "...1表紙.png"
python3 tools/generate_image.py --prompt "..." --output "...2白ブラウス.png"
# ...
```

### ステップ3: ファイル命名規則

```
[Week番号]_[月日]_[テーマ略称]_[連番とテーマ].png

例:
Week3_4月1日_春の3色ルール_1表紙.png
Week3_4月1日_春の3色ルール_2白ブラウス.png
Week3_4月5日_つくっている理由.png
```

### ステップ4: 完了報告

```
✅ 画像生成完了。以下を保存しました：
- マーケティング/Instagram/画像/Week3_4月1日_春の3色ルール_1表紙.png（2.8MB）
- マーケティング/Instagram/画像/Week3_4月1日_春の3色ルール_2白ブラウス.png（2.9MB）
（以下続く）
```

---

## スクリプト仕様（`tools/generate_image.py`）

| 引数 | 説明 | デフォルト |
|------|------|---------|
| `--prompt` | 画像プロンプト（必須） | - |
| `--output` | 保存先パス `.png`（必須） | - |
| `--aspect` | `1:1` / `9:16` / `16:9` | `1:1` |
| `--count` | 生成枚数（同一プロンプトで複数欲しい場合） | `1` |

- **ブランドスタイルは自動付加**: プロンプトに「水彩タッチ・tenue世界観」が自動追加される
- **保存先ディレクトリがなければ自動作成**
- **GEMINI_API_KEY は `.env` から自動読み込み**

---

## 画像スタイルガイド（tenue ブランド）

| 要素 | 仕様 |
|------|------|
| スタイル | 水彩タッチのイラスト風 |
| 雰囲気 | 少女マンガ的な繊細さ・温かみ |
| カラーパレット | アイボリー・ベージュ・テラコッタ・くすみピンク・ペールブルー |
| 人物 | 20〜30代の日本人女性（横顔・後ろ姿推奨） |
| テキスト | **なし**（no text 必須） |
| 画質 | 高品質・Instagram映え |

---

## よくある失敗とリカバリー

| 問題 | 対処 |
|------|------|
| `GEMINI_API_KEY が設定されていません` | `.env` にキーを設定（[取得先](https://aistudio.google.com/apikey)） |
| `画像が生成されませんでした` | プロンプトから具体的な人物描写を減らして再実行 |
| テキストが画像に混入 | プロンプトに「no text, no letters, 文字なし」を追加 |
| スタイルがリアル寄り | プロンプトに「水彩タッチ・アニメイラスト風・ソフトなタッチ」を追加 |
| カラーがブランドと合わない | 「アイボリー・ベージュ・テラコッタのみ」を明示 |
| 縦横比が違う | `--aspect 9:16` を確認 |

---

## 参照ファイル

- `tools/generate_image.py` — 実行スクリプト本体
- `docs/ブランド.md` — カラー・世界観の詳細
- `マーケティング/Instagram/投稿予定_[週].md` — プロンプト源泉
- `マーケティング/Instagram/画像/` — 保存先
